/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Si besoin d'images externes, ajouter les domaines ici
    // domains: ['example.com'],
  },
}

module.exports = nextConfig

